# 事件判断系统整体逻辑文档

## 1. 系统概述

本系统基于YOLO目标检测与跟踪技术，实现对交通场景中四种事件的自动检测：
- **拥堵事件（Jam）**：检测交通拥堵情况
- **停车事件（Park）**：检测违规停车行为
- **行人事件（People）**：检测行人出现
- **故障事件（Breakdown）**：检测车辆故障（长时间停车）

系统采用逐帧处理的方式，通过目标跟踪获取车辆运动信息，结合速度、位置、类别等多维度特征进行事件判断。

---

## 2. 系统架构

### 2.1 核心模块

```
┌─────────────────┐
│   EventDet.py   │  ← 主控制器：视频处理、目标跟踪、事件协调
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    judger.py    │  ← 判断器：具体事件判断逻辑
└─────────────────┘
```

### 2.2 数据流

```
视频输入 → YOLO检测跟踪 → 速度计算 → 事件判断 → 稳定判定 → 输出结果
```

---

## 3. 核心算法流程

### 3.1 主处理流程（EventDet.py）

```pseudocode
算法：事件检测主流程
输入：视频路径 video_path
输出：带标注的视频和JSON事件数据

BEGIN
    // 初始化
    加载YOLO模型
    打开视频文件
    获取视频FPS、宽度、高度
    初始化事件状态跟踪器（拥堵、停车、行人、故障）
    计算稳定判定所需帧数（基于FPS和1秒阈值）
    
    // 逐帧处理
    FOR 每一帧 frame IN 视频:
        // 1. 目标检测与跟踪
        tracks = YOLO模型.track(frame)
        
        // 2. 初始化当前帧事件结果
        jam_result = [False, False, False, False]  // [jam, park, people, breakdown]
        detected_objects = []
        judger.jam_vehicle_info = []  // 清空拥堵候选车辆列表
        
        // 3. 处理每个检测到的目标
        FOR 每个目标 track IN tracks:
            // 3.1 提取目标信息
            track_id = track.id
            bbox = track.bbox
            class_name = track.class
            confidence = track.confidence
            current_position = bbox中心点
            
            // 3.2 计算速度
            prev_data = 查找历史数据(track_id)
            speed = 计算速度(prev_data, current_position, fps)
            
            // 3.3 构建当前数据
            current_data = {
                id: track_id,
                class: class_name,
                x: current_position.x,
                y: current_position.y,
                size_w: bbox宽度,
                size_h: bbox高度,
                speed: speed,
                frame: 当前帧号,
                fps: 视频帧率,
                confidence: confidence
            }
            
            // 3.4 事件判断（单个目标）
            judger.current_data = current_data
            judger.prev_data = prev_data
            judger.result = [False, False, False, False]
            judger.main()  // 执行判断逻辑
            
            // 3.5 累积事件状态（OR逻辑）
            IF judger.result[1]:  // 停车
                jam_result[1] = True
            IF judger.result[2]:  // 行人
                jam_result[2] = True
            IF judger.result[3]:  // 故障
                jam_result[3] = True
            
            // 3.6 更新车辆历史数据
            更新vehicle_data(track_id, current_data)
        
        // 4. 拥堵判断（需要处理完所有车辆后统一判断）
        IF NOT jam_result[0]:
            judger.checkJam()  // 基于所有慢速车辆的空间分布判断
            jam_result[0] = judger.result[0]
            IF jam_result[0]:
                jam_result[1] = False  // 拥堵时不是停车
        
        // 5. 事件稳定判定（时间门限与滞回）
        jam_result = 稳定判定(jam_result, 稳定帧数)
        
        // 6. 输出结果
        绘制检测框和事件信息到frame
        保存事件数据到frame_events
        写入输出视频
        
    END FOR
    
    // 保存JSON文件
    保存事件数据到JSON文件
    
END
```

### 3.2 事件判断核心逻辑（judger.py）

#### 3.2.1 速度阈值计算

```pseudocode
算法：计算速度阈值（基于检测框尺寸和车辆类型归一化）
输入：current_data（包含车辆类别、检测框尺寸）
输出：速度阈值（像素/帧）

BEGIN
    vehicle_class = current_data.class
    size = current_data.size_w  // 或size_h（卡车用高度）
    
    // 获取车辆类型归一化因子（消除车辆类型对检测框尺寸的影响）
    normalization_factor = VEHICLE_NORMALIZATION_FACTOR[vehicle_class]
    
    // 归一化检测框尺寸
    normalized_size = size * normalization_factor
    
    // 限制归一化尺寸范围（避免极端值）
    IF normalized_size < MIN_DETECTION_SIZE:
        normalized_size = MIN_DETECTION_SIZE
    ELSE IF normalized_size > MAX_DETECTION_SIZE:
        normalized_size = MAX_DETECTION_SIZE
    
    // 计算速度阈值
    min_speed_threshold = normalized_size * MIN_SPEED_WEIGHT  // 停车判断用
    slow_speed_threshold = normalized_size * SLOW_SPEED_WEIGHT  // 拥堵判断用
    
    RETURN min_speed_threshold, slow_speed_threshold
END
```

**关键思想**：
- 检测框尺寸主要反映距离，但也受车辆类型影响
- 通过归一化因子将不同车辆类型的检测框转换为"等效距离"
- 确保同一距离下不同车辆类型的速度阈值相近

#### 3.2.2 停车事件判断

```pseudocode
算法：判断停车事件
输入：current_data, prev_data
输出：是否停车（布尔值）

BEGIN
    // 1. 验证车辆类型
    IF current_data.class NOT IN ["car", "truck"]:
        RETURN False
    
    // 2. 计算速度阈值
    speed_threshold = 计算最小速度阈值(current_data)
    
    // 3. 计算时间增量
    IF 有帧号信息:
        dt = (current_frame - prev_frame) / fps
    ELSE:
        dt = current_time - prev_time
    
    // 4. 判断当前速度是否低于阈值
    is_low_speed = current_data.speed <= speed_threshold
    
    // 5. 累积低速持续时间
    IF is_low_speed:
        current_duration = prev_duration + dt
    ELSE:
        current_duration = 0.0
    
    current_data.low_speed_duration = current_duration
    
    // 6. 判定停车（需要同时满足：当前速度低 + 持续时间≥3秒）
    IF is_low_speed AND current_duration >= STOP_DURATION_SECONDS:
        RETURN True
    ELSE:
        RETURN False
END
```

**关键特点**：
- 使用持续时间判定，避免瞬时低速误判
- 需要连续3秒速度低于阈值才判定为停车
- 速度高于阈值时立即重置持续时间

#### 3.2.3 拥堵事件判断

```pseudocode
算法：判断拥堵事件
输入：jam_vehicle_info（当前帧所有慢速车辆信息列表）
输出：是否拥堵（布尔值）

BEGIN
    // 1. 检查慢速车辆数量
    IF len(jam_vehicle_info) < MAX_JAM_VEHICLE_NUM:
        RETURN False
    
    // 2. 按配置轴（x或y）对车辆排序
    axis = jam_axis  // 'x' 或 'y'
    sorted_vehicles = 按axis坐标排序(jam_vehicle_info)
    
    // 3. 查找连续紧邻的车辆序列
    consecutive_count = 1
    FOR i = 1 TO len(sorted_vehicles) - 1:
        prev_vehicle = sorted_vehicles[i-1]
        curr_vehicle = sorted_vehicles[i]
        
        // 计算轴向间距
        axis_distance = |curr_vehicle[axis] - prev_vehicle[axis]|
        
        // 计算间距阈值（基于车辆宽度）
        gap_threshold = min(prev_vehicle.width, curr_vehicle.width) * MAX_VEHICLE_GAP_WEIGHT
        
        // 判断是否紧邻
        IF axis_distance < gap_threshold:
            consecutive_count = consecutive_count + 1
            IF consecutive_count >= MAX_JAM_VEHICLE_NUM:
                RETURN True  // 找到足够长的连续序列
        ELSE:
            consecutive_count = 1  // 重置计数
    
    RETURN False
END
```

**关键特点**：
- 需要至少20辆慢速车辆
- 按指定轴（x或y）排序，符合排队方向
- 使用车辆宽度成比例的间距阈值判断紧邻
- 需要连续紧邻的车辆序列达到阈值数量

#### 3.2.4 行人事件判断

```pseudocode
算法：判断行人事件
输入：current_data
输出：是否检测到行人（布尔值）

BEGIN
    IF current_data.class == "person" AND current_data.confidence >= 0.8:
        RETURN True
    ELSE:
        RETURN False
END
```

**关键特点**：
- 需要类别为"person"
- 需要置信度≥0.8（提高检测可靠性）

#### 3.2.5 故障事件判断

```pseudocode
算法：判断故障事件
输入：current_data
输出：是否故障（布尔值）

BEGIN
    // 1. 必须先满足停车条件
    IF NOT isParking():
        RETURN False
    
    // 2. 检查停车持续时间
    current_duration = current_data.low_speed_duration
    
    IF current_duration >= BREAKDOWN_DURATION_SECONDS:  // 180秒（3分钟）
        RETURN True
    ELSE:
        RETURN False
END
```

**关键特点**：
- 故障是停车的特殊情况
- 需要停车时间超过3分钟（180秒）
- 基于停车判断中累积的持续时间

### 3.3 事件稳定判定机制

```pseudocode
算法：事件稳定判定（时间门限与滞回）
输入：当前帧事件结果 jam_result，稳定判定帧数
输出：稳定后的事件状态

BEGIN
    // 对每种事件类型分别进行稳定判定
    FOR 每种事件类型 event_type IN [jam, park, people, breakdown]:
        current_state = jam_result[event_type]
        
        IF current_state:
            // 事件满足条件
            consecutive_count[event_type] = consecutive_count[event_type] + 1
            no_consecutive_count[event_type] = 0
            
            // 如果连续满足条件达到阈值，且之前未激活，则激活事件
            IF NOT event_state[event_type] AND consecutive_count[event_type] >= confirm_frames:
                event_state[event_type] = True
        ELSE:
            // 事件不满足条件
            no_consecutive_count[event_type] = no_consecutive_count[event_type] + 1
            consecutive_count[event_type] = 0
            
            // 如果连续不满足条件达到阈值，且之前已激活，则解除事件
            IF event_state[event_type] AND no_consecutive_count[event_type] >= clear_frames:
                event_state[event_type] = False
        
        // 使用稳定后的状态
        jam_result[event_type] = event_state[event_type]
    END FOR
    
    RETURN jam_result
END
```

**关键特点**：
- 所有事件都需要持续1秒（event_on_seconds）才判定为发生
- 所有事件都需要持续1秒不满足条件（event_off_seconds）才解除
- 使用滞回机制，避免事件状态频繁切换
- 基于FPS动态计算所需帧数

---

## 4. 事件优先级与互斥关系

### 4.1 事件优先级

```
拥堵 > 停车 > 行人/故障
```

### 4.2 互斥关系

1. **拥堵与停车**：互斥
   - 当检测到拥堵时，停车事件被抑制（`jam_result[1] = False`）
   - 逻辑：拥堵时所有车辆都慢速，不应判定为停车

2. **其他事件**：可同时发生
   - 拥堵 + 行人：可同时发生
   - 停车 + 行人：可同时发生
   - 停车 + 故障：可同时发生（故障是停车的特殊情况）

### 4.3 输出消息规则

```pseudocode
算法：生成事件输出消息
输入：稳定后的事件状态 jam_result
输出：事件消息列表

BEGIN
    event_messages = []
    
    // 按优先级添加消息
    IF jam_result[0]:  // 拥堵
        event_messages.append("jam!")
    
    IF jam_result[3]:  // 故障
        event_messages.append("breakdown!")
    
    IF jam_result[1] AND NOT jam_result[0]:  // 停车（且非拥堵）
        event_messages.append("there are parked cars!")
    
    IF jam_result[2]:  // 行人
        event_messages.append("there are people!")
    
    // 无事件时显示正常状态
    IF event_messages为空:
        event_messages.append("everything is ok")
    
    RETURN event_messages
END
```

---

## 5. 关键参数配置

### 5.1 速度阈值参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `MIN_SPEED_WEIGHT` | 0.02 | 停车判断的最小速度权重（相对于检测框尺寸） |
| `SLOW_SPEED_WEIGHT` | 0.25 | 拥堵判断的慢速权重（相对于检测框尺寸） |

### 5.2 拥堵判断参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `MAX_JAM_VEHICLE_NUM` | 20 | 判定拥堵所需的最少慢速车辆数 |
| `MAX_VEHICLE_GAP_WEIGHT` | 3 | 车辆间距阈值权重（相对于车辆宽度） |

### 5.3 停车判断参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `STOP_DURATION_SECONDS` | 3.0 | 判定停车所需的速度持续低于阈值的时间（秒） |

### 5.4 故障判断参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `BREAKDOWN_DURATION_SECONDS` | 180.0 | 判定故障所需的停车持续时间（秒，即3分钟） |

### 5.5 事件稳定判定参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `event_on_seconds` | 1.0 | 事件激活所需持续时间（秒） |
| `event_off_seconds` | 1.0 | 事件解除所需持续时间（秒） |

### 5.6 车辆类型归一化因子

| 车辆类型 | 归一化因子 | 说明 |
|---------|-----------|------|
| car | 1.2 | 参考车辆 |
| truck | 0.5 | 卡车检测框更大，需要缩小 |
| bus | 0.80 | 公交车检测框更大，需要缩小 |
| motorcycle | 1.3 | 摩托车检测框更小，需要放大 |
| default | 1.0 | 默认值 |

---

## 6. 数据输出格式

### 6.1 JSON输出结构

```json
{
    "video_filename": "视频文件名",
    "fps": 帧率,
    "total_frames": 总帧数,
    "result": [
        {
            "frame": 帧号,
            "timestamp": 时间戳（秒）,
            "event": {
                "jam": 是否拥堵（布尔值）,
                "parked": 是否停车（布尔值）,
                "people": 是否行人（布尔值）,
                "breakdown": 是否故障（布尔值）
            },
            "objects": [
                {
                    "track_id": 跟踪ID,
                    "class_id": 类别ID,
                    "class_name": 类别名称,
                    "confidence": 置信度,
                    "bbox": {
                        "x_min": 左上角x,
                        "y_min": 左上角y,
                        "x_max": 右下角x,
                        "y_max": 右下角y
                    },
                    "center": {
                        "x": 中心点x,
                        "y": 中心点y
                    },
                    "size": {
                        "width": 宽度,
                        "height": 高度
                    },
                    "speed": 速度（像素/秒）
                }
            ]
        }
    ]
}
```

### 6.2 可视化输出

- 检测框绘制：
  - 正常车辆：绿色框
  - 停车车辆：橙色框（加粗）
  - 行人：根据置信度显示
- 事件信息面板：
  - 帧数、时间、FPS、目标数量
  - 事件状态指示器（拥堵、停车、行人、故障）
- 事件消息：
  - 在视频左上角显示事件消息
  - 红色表示有事件，绿色表示正常

---

## 7. 算法优化点

### 7.1 速度阈值自适应

- **问题**：不同距离的车辆，相同像素速度对应的实际速度不同
- **解决**：基于检测框尺寸动态计算速度阈值，距离越远阈值越小

### 7.2 车辆类型归一化

- **问题**：不同车辆类型在相同距离下检测框尺寸不同
- **解决**：使用归一化因子消除车辆类型影响，确保同一距离下阈值一致

### 7.3 事件稳定判定

- **问题**：瞬时事件导致误报
- **解决**：使用时间门限和滞回机制，需要持续1秒才判定为事件发生

### 7.4 拥堵判断优化

- **问题**：需要基于所有车辆的空间分布判断
- **解决**：先收集所有慢速车辆信息，处理完所有车辆后统一判断

---

## 8. 总结

本系统通过以下关键技术实现了准确的事件检测：

1. **多维度特征融合**：结合速度、位置、类别、置信度等多维度信息
2. **自适应阈值**：基于检测框尺寸和车辆类型动态调整速度阈值
3. **时间序列分析**：使用持续时间判定，避免瞬时误判
4. **空间分布分析**：通过车辆空间分布判断拥堵
5. **稳定判定机制**：使用时间门限和滞回，提高检测稳定性

系统能够有效检测交通场景中的拥堵、停车、行人和故障四种事件，为交通管理提供可靠的数据支持。

